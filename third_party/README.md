# Third-Party Inputs

Place downloaded benchmark archives here when you want the launchers to use them locally.

Downloaded files in this directory are ignored by git, Docker build context uploads, and GCP source bundles so large vendor files and licensed benchmark inputs do not get committed accidentally.
